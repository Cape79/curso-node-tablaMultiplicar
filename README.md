#Notas:
Mi primer programa en node.
```
Options:
      --help     Show help                [boolean]
      --version  Show version number      [boolean]
  -b, --base     Es la base para multiplicar
                                [number] [required]
  -l, --listar   Muestra por pantalla la tabla
                         [boolean] [default: false]
```